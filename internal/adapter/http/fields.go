package adapthttp

// Shared string constants — deduped to satisfy goconst (cookie name + JSON keys).
const (
	cookieSession = "session"
	fieldStatus   = "status"
	fieldToday    = "today"
	fieldItems    = "items"
	fieldEntry    = "entry"
)
